Комментарии:
1. sp_email_get_biz_list_for_menu
немного неверно, ранжировать бизнесы нужно не на основании таблицы
email_users, а на основании таблицы biz_users, в которой если установлено
is_driver = 1 значит пользователь driver, иначе - навигатор.

2. email_main.class.php
559:
$is_dfa     = Request::GetBoolean('is_dfa',     $_REQUEST);
$approve_by = $is_dfa ? $this->user_id : 0;

таких фильтров при поиске писем не будет.